# SVGDO 部署指南

> 域名：**svgdo.com**
> 平台：Cloudflare Pages
> GitHub：https://github.com/humenglover/svgdo.git

---

## 一、打包

```bash
npm run build
```

构建流程：`tsc -b`（类型检查）→ `vite build`（打包）→ `generate-sitemap.cjs`（站点地图）

打包后文件在 `dist/` 目录，包含：
- `index.html` — 入口页面
- `assets/` — JS/CSS 静态资源
- `content/` — Markdown 文章（5 语言）
- `icons/` — SVG 图标
- `_redirects` — SPA 路由重定向规则
- `sitemap.xml` / `robots.txt` / `favicon.svg` / `logo.svg`

---

## 二、部署到 Cloudflare Pages

```bash
npx wrangler pages deploy dist --project-name=svgdo
```

部署完成后站点地址：`https://svgdo.pages.dev` → 自定义域名 `https://svgdo.com`

---

## 三、常用命令

```bash
npm run dev          # 本地开发（端口 5174）
npm run build        # 生产打包
npm run preview      # 本地预览构建产物
npm run lint         # TypeScript 类型检查
npm run build && npx wrangler pages deploy dist --project-name=svgdo  # 一键打包部署
```

---

## 四、站点地图

- 地址：`https://svgdo.com/sitemap.xml`
- Google Search Console 提交：`https://svgdo.com/sitemap.xml`
- 每次 `npm run build` 自动在 `public/sitemap.xml` 和 `dist/sitemap.xml` 生成

---

## 五、域名

- 注册商：Spaceship
- DNS / Pages：Cloudflare
- `.com` 域名，GSC 无兼容问题

---

## 六、SEO 多语言

项目支持 5 种语言：中文、English、日本語、한국어、Español

- 英文为默认语言，og:locale 同时声明 5 种
- 文章路径：`/article/svg-js-interaction`（自动根据语言加载对应 `.md` 文件）
- 内容文件命名：`xxx.md`（英文默认）、`xxx.zh.md`、`xxx.ja.md`、`xxx.ko.md`、`xxx.es.md`

---

## 七、项目结构

```
svg-editor/
├── public/                  # 静态资源（构建时直接复制到 dist/）
│   ├── content/             # Markdown 文章（5 语言）
│   ├── icons/               # SVG 图标库（100+ 精选图标）
│   ├── favicon.svg
│   ├── logo.svg
│   ├── robots.txt
│   └── sitemap.xml
├── src/                     # React 源代码
│   ├── App.tsx              # 主应用入口
│   ├── pages/               # 页面组件（Editor/ArticlePage/Resources/NotFound）
│   ├── components/          # 可复用组件
│   ├── data/articles.ts     # 文章元数据
│   └── locales/             # i18n 翻译（zh/en/ja/ko/es）
├── dist/                    # 构建产物（部署目标）
├── scripts/                 # 构建辅助脚本
│   └── generate-sitemap.cjs
├── vite.config.ts
└── package.json
```
